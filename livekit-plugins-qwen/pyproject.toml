[project]
name = "livekit-plugins-qwen"
version = "0.1.0"
description = "LiveKit Agents plugin for Alibaba Qwen-Omni-Realtime via DashScope"
requires-python = ">=3.11"
dependencies = [
    "livekit-agents>=1.0.0",
    "aiohttp>=3.9.0",
]

[dependency-groups]
dev = []
